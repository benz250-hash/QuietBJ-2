v506 四项联改：
1. 轨道交通拆分为：地上轨道 / 轨道站点 / 地铁出入口。
2. app 的 around 查询拆为 school / hospital / commercial / restaurant / rail_station / rail_entrance。
3. shielding_engine 保留缓存配合逻辑，但 build_shielding_result 改成只返回 evidence；真正遮挡修正在 score_engine 内完成。
4. 顶部第一格改为“本地生活噪音”，result_dict / noise_summary 适配 penalty、raw_penalty、遮挡明细。

说明：
- 这一版为了守住“主引擎结算”，额外把 building_engine 也改成 evidence-only。
- zone / override 目前仍保留证据对象，但默认退出主评分链。
